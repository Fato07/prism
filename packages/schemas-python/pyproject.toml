[project]
name = "prism-schemas"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.13", "structlog>=24.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/prism_schemas"]
